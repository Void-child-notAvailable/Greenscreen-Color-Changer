Run this with no arguments in command line for custom color picker.


Run with command line arguments exampleImage.png colors.json (see example) for automated usage.

E.g.
python ChromaKey.py 

python ChromaKey.py img.jpg example.json
